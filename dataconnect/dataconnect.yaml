specVersion: "v1beta"
serviceId: "game-lang-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "game-lang-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
